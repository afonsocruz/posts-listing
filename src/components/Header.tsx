import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="w-full h-28 flex items-center justify-center bg-slate-200">
      <h3 className="text-2xl font-semibold">Posts from API</h3>
    </header>
  );
};

export default Header;
